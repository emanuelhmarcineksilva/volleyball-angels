/*document.addEventListener('DOMContentLoaded', () => {
    // Seleciona o contêiner do conteúdo que vai rolar
    const conteudoAnimado = document.querySelector('.faixa-amarela .faixa-conteudo');

    // Calcula a largura total do conteúdo
    const larguraTotal = conteudoAnimado.scrollWidth;

    // A distância da animação é metade da largura total (porque duplicamos o conteúdo)
    const distanciaAnimacao = larguraTotal / 2;

    anime({
        targets: conteudoAnimado,
        // Anima o eixo X da posição 0 até a metade da largura
        translateX: [0, -distanciaAnimacao],
        duration: 20000, // Aumente para uma animação mais lenta, diminua para mais rápida
        easing: 'linear',
        loop: true
    });
});*/

